# قاعدة عنوان الـ API (الـ backend Laravel)
# الإنتاج (Coolify env var عند البناء، لا يُقرأ من هذا الملف): VITE_API_BASE_URL=https://api.harer.store/api/v1
VITE_API_BASE_URL=http://localhost:8000/api/v1
