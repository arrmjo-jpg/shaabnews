# ── AlphaCMS API edge (nginx → php-fpm FastCGI) ────────────────────────────────
# php-fpm (backend:9000) يتكلّم FastCGI لا HTTP. Traefik/Coolify يوجّه api.<domain> إلى هذه
# الخدمة على المنفذ 80، وتُترجم كلّ طلب HTTP إلى FastCGI عبر public/index.php. الموقع العامّ
# (Next.js) يُخدَم مباشرةً من Traefik → frontend:3000 (لا يمرّ من هنا). TLS يُنهى عند الحافة.

gzip on;
gzip_comp_level 5;
gzip_min_length 1024;
gzip_vary on;
gzip_proxied any;
gzip_types text/plain text/css application/json application/javascript application/xml
           application/xml+rss application/rss+xml image/svg+xml text/xml application/manifest+json;

server {
    listen 80 default_server;
    server_name _;                      # Traefik يوجّه api.<domain> فقط إلى هنا
    client_max_body_size 512M;          # رفع وسائط الإدارة (فيديو/صور)

    # الوسائط المرفوعة (قرص 'public' المحلّيّ) — تُخدَم من حجم media المشترك.
    location /storage/ {
        alias /srv/media/;
        access_log off;
        expires 30d;
        add_header Cache-Control "public";
        try_files $uri =404;
    }

    # كلّ ما عداها → موجّه Laravel الأماميّ عبر FastCGI على php-fpm.
    location / {
        fastcgi_pass backend:9000;
        fastcgi_param SCRIPT_FILENAME /var/www/html/public/index.php;
        fastcgi_param SCRIPT_NAME /index.php;
        fastcgi_param DOCUMENT_ROOT /var/www/html/public;
        include fastcgi_params;
        fastcgi_param HTTP_PROXY "";    # تخفيف httpoxy
        fastcgi_read_timeout 120s;
    }
}
