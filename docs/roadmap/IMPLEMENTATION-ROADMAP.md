# خارطة طريق التنفيذ — واجهة Next.js العامة

**وثيقة تنفيذ واحدة تُجمِّع نتائج التقارير الثلاثة**
([FRONTEND-AUDIT.md](FRONTEND-AUDIT.md)،
[FRONTEND-ARCHITECTURE-VALIDATION.md](FRONTEND-ARCHITECTURE-VALIDATION.md)،
[FRONTEND-RUNTIME-AUDIT.md](FRONTEND-RUNTIME-AUDIT.md)) في خطّة تنفيذ
مُرتَّبة بـ 4 مراحل (Sprint 0–3) حسب العائد (ROI)، لا الخطورة فقط.

## Changelog

- **v1**: خارطة أوّليّة — 3 Sprints، مرتَّبة حسب ROI.
- **v2**: Sprint 0 (Baseline)، نقل `React.cache()` لِـ Sprint 2، رفع
  أولويّة SEO، بند API Contract Snapshot، Type + معايير القبول لكل بند،
  Feature Flags، Risk Matrix.
- **v3**: Definition of Done لكل Sprint، خطط تراجع (Rollback)، إثراء
  Contract Snapshot بالـ Headers/Status، Performance Budget، Owner
  لكل بند، Dependencies، KPI لكل Sprint، عمود Blast Radius، Changelog.
- **v4 (الحاليّة)**: سجلّ قرارات مركزيّ منفصل
  ([DECISION-LOG.md](../architecture/DECISION-LOG.md))، تصنيف تشغيليّ
  لكل بند (Safe Change/Low Risk/High Risk/Maintenance Window/Production
  Monitoring)، قائمة نجاح موحَّدة لما بعد النشر (24 ساعة)، قائمة مراقبة
  (Monitoring Checklist) للبنود عالية الأثر، وربط صريح بالقرارات/ADRs
  المرشَّحة.

كل بند: **النوع**، **المسؤول**، **🔧 التصنيف التشغيليّ**، السبب،
**التبعيّات**، الوقت المتوقَّع، المخاطرة، التأثير، الملفّات المتأثّرة،
Backend/Frontend/Migration/إعادة اختبار، **معايير القبول**، **🚩 Feature
Flag**، **↩ خطّة التراجع**، **📡 المراقبة** (للبنود عالية الأثر)، و**🔗
القرار/ADR** (حيث ينطبق) — حيث يلزم كلّ منها.

---

## ملاحظات منهجيّة

1. **بند تضخّم ترميز Unicode لن يُنفَّذ مباشرة** — تحقّق (1.1) ثمّ تنفيذ
   (1.2) فقط بناءً على النتيجة.
2. **أي رقم أداء يجب تمييز Cold-cache عن Warm-cache صراحة.**
3. **لا قياس بلا خطّ أساس** — Sprint 0 قبل أي تنفيذ.
4. **الأولويّة: أخطاء حيّة مؤكَّدة ← SEO عالي الأثر ← شبكة أمان العقد.**
5. **لا Sprint "منتهٍ" لمجرّد انتهاء مهامّه** — Definition of Done صريح
   لكل Sprint.
6. **لا قرار "ضائع بين الاجتماعات"** — أي بند يعتمد على قرار بديل حقيقيّ
   (لا قرار هندسيّ روتينيّ) مسجَّل في
   [DECISION-LOG.md](../architecture/DECISION-LOG.md)، لا داخل هذه
   الوثيقة فقط.
7. **الرجوع (Rollback) يُجيب "ماذا لو فشل؟"؛ المراقبة (Monitoring) تُجيب
   "كيف سنعرف أنّه بدأ يفشل؟"** — حقلان مختلفان، ليسا مترادفَين.

---

## قائمة تحقّق موحَّدة لما بعد النشر (24 ساعة) — تنطبق على كل بند مصنَّف "Requires Production Monitoring"

- ✓ لا زيادة في معدَّل استجابات 5xx مقارنةً بمتوسّط الـ 24 ساعة السابقة
  للنشر.
- ✓ لا زيادة في أخطاء JS الحيّة (console errors) على الصفحات المتأثِّرة.
- ✓ لا زيادة في زمن استجابة الـ API (p50/p95) للـ endpoints المتأثِّرة.
- ✓ لم يُفعَّل أي Rollback (يدويّ أو تلقائيّ) خلال النافذة.

يُشار إليها أدناه اختصاراً بـ "**✅ نجاح 24 ساعة: القائمة الموحَّدة**"،
مع أي إضافة خاصّة بالبند إن وُجدت.

---

# Sprint 0 — Baseline & Observability

**الهدف الوحيد: توثيق الحالة الحاليّة رقميّاً، بلا أي تغيير كود.**

### 0.1 قياس Lighthouse (Desktop + Mobile)

- **النوع**: Observability. **المسؤول**: Frontend + DevOps.
- **🔧 التصنيف التشغيليّ**: Safe Change (قياس فقط، بلا نشر).
- **السبب**: لا يوجد قياس Lighthouse رسميّ حتى الآن.
- **التبعيّات**: لا شيء — أوّل بند يُنفَّذ.
- **الإجراء**: `npx lighthouse <url> --preset=desktop --output=json --output-path=baseline-desktop.json` ثمّ بلا `--preset` للجوّال، على بناء
  إنتاجيّ (`next build`+`next start`) حصراً.
- **الوقت المتوقَّع**: 30 دقيقة. **المخاطرة**: صفر.
- **الملفّات المتأثّرة**: لا كود؛ حفظ في `docs/roadmap/baselines/`.
- Backend؟ لا — Frontend؟ لا — Migration؟ لا — إعادة اختبار؟ لا.
- **معايير القبول**: ملفّا JSON محفوظان بدرجات كاملة.

### 0.2 Web Vitals الحقيقيّة (LCP، CLS، INP)

- **النوع**: Observability. **المسؤول**: Frontend + DevOps.
- **🔧 التصنيف التشغيليّ**: Safe Change.
- **السبب**: تقرير التشغيل رصد مؤشِّر CLS محتمل (`/bourse`) بلا رقم مقاس.
- **التبعيّات**: يُستخرَج من نتائج 0.1.
- **الوقت المتوقَّع**: 15 دقيقة. **المخاطرة**: صفر.
- **التأثير**: يُحدِّد أولويّة 3.2 الفعليّة.
- Backend؟ لا — Frontend؟ لا — Migration؟ لا — إعادة اختبار؟ لا.
- **معايير القبول**: جدول LCP/CLS/INP للرئيسية، مقال، قسم، و`/bourse`.

### 0.3 حجم JS وعدد Requests وحجم JSON

- **النوع**: Observability. **المسؤول**: Frontend.
- **🔧 التصنيف التشغيليّ**: Safe Change.
- **السبب**: موجود جزئيّاً بالفعل في FRONTEND-RUNTIME-AUDIT.md — يُنسَّق
  هنا في ملفّ واحد رسميّ.
- **التبعيّات**: لا شيء، بالتوازي مع 0.1/0.2.
- **الوقت المتوقَّع**: 45 دقيقة – 1 ساعة. **المخاطرة**: صفر.
- **الملفّات المتأثّرة**: `docs/roadmap/baselines/BASELINE-METRICS.md`.
- Backend؟ لا — Frontend؟ لا — Migration؟ لا — إعادة اختبار؟ لا.
- **معايير القبول**: رقم محدَّد لكل مقياس، مع تاريخ ونوع البناء.

### 0.4 Performance Budget

- **النوع**: Observability / Policy. **المسؤول**: Frontend + DevOps.
- **🔧 التصنيف التشغيليّ**: Safe Change.
- **السبب**: بلا أهداف رقميّة، "أفضل من قبل" ليس "جيّداً بما يكفي".
- **التبعيّات**: يعتمد على أرقام 0.1–0.3.
- **الميزانيّة المقترَحة**:

  | المقياس | الميزانيّة | الوضع الحاليّ المقاس | الحالة |
  |---|---|---|---|
  | First Load JS (أثقل صفحة عامّة) | ≤ 220 كيلوبايت | `/bourse` = 226 كيلوبايت | **يتجاوز اليوم** |
  | First Load JS (صفحات حساب/كتابة) | استثناء موثَّق (خلف مصادقة) | `/account/content/new` = 265 كيلوبايت | مُستثنى بقرار صريح |
  | حجم JSON للرئيسية | ≤ 70 كيلوبايت | 69.9 كيلوبايت خام (39.3 بعد 1.2) | عند الحدّ اليوم |
  | حجم JSON لتفاصيل مقال | ≤ 35 كيلوبايت | 19.3 كيلوبايت خام | **ضمن الميزانيّة** |
  | LCP | < 2.5 ثانية | يُقاس في 0.2 | يُحدَّد لاحقاً |
  | CLS | < 0.1 | يُقاس في 0.2 | يُحدَّد لاحقاً |
  | INP | < 200 ميلي ثانية | يُقاس في 0.2 | يُحدَّد لاحقاً |

- **الوقت المتوقَّع**: 30 دقيقة. **المخاطرة**: صفر.
- **الملفّات المتأثّرة**: قسم في `BASELINE-METRICS.md`.
- Backend؟ لا — Frontend؟ لا — Migration؟ لا — إعادة اختبار؟ لا.
- **معايير القبول**: الجدول مُعتمَد رسميّاً (commit)، يُستخدَم كمعيار
  Definition-of-Done لكل Sprint لاحق.

### Definition of Done — Sprint 0

- ✓ الملفّات الأربعة محفوظة بتاريخ وربط commit واضحين.
- ✓ Performance Budget معتمَد في commit منفصل.
- ✓ كل قياس على بناء إنتاجيّ موثَّق صراحة.
- ✓ صفر تعديل كود خلال هذه المرحلة.

**KPI**: 100% من المقاييس السبعة موثَّقة ومحفوظة.

---

# Sprint 1 — أعلى عائد (ROI)، أقلّ مخاطرة، أخطاء حيّة + SEO أوّلاً

### 1.1 التحقّق من فرضية تضخّم الترميز قبل أي تنفيذ

- **النوع**: Investigation. **المسؤول**: Backend.
- **🔧 التصنيف التشغيليّ**: Safe Change.
- **السبب**: تضخّم 1.78–2.1× مقاس؛ الأثر الشبكيّ يعتمد على ضغط الإنتاج.
- **التبعيّات**: لا شيء.
- **خطوات التحقّق**: (1) تأكيد إعداد Cloudflare لضغط `/api/*`. (2) `curl
  -H "Accept-Encoding: br, gzip" -D - <domain>/api/v1/ar/homepage`. (3)
  توثيق النتيجة.
- **الوقت المتوقَّع**: 30–60 دقيقة. **المخاطرة**: صفر.
- Backend؟ لا — Frontend؟ لا — Migration؟ لا — إعادة اختبار؟ لا.
- **معايير القبول**: إجابة موثَّقة على السؤالين.

### 1.2 تنفيذ `JSON_UNESCAPED_UNICODE` (بعد 1.1 فقط)

- **النوع**: Optimization (Backend). **المسؤول**: Backend.
- **🔧 التصنيف التشغيليّ**: **Safe Change** + **Production Monitoring:
  Yes** (Blast Radius واسع — كامل الـ API — رغم بساطته التقنيّة).
- **السبب**: نتيجة 1.1. **التبعيّات**: يعتمد على 1.1.
- **الوقت المتوقَّع**: 15–30 دقيقة. **المخاطرة**: منخفضة جدّاً.
- **الملفّات المتأثّرة**: نقطة تصيير JSON المركزيّة في Laravel.
- Backend؟ **نعم** — Frontend؟ لا — Migration؟ لا — إعادة اختبار؟ نعم.
- **معايير القبول**: UTF-8 خام لا `\uXXXX`؛ حجم استجابة منخفض بمقدار
  مطابق لـ1.1 (±5%)؛ صفر اختلاف باختبارات الواجهة.
- **↩ خطّة التراجع**: إزالة الخيار → إعادة نشر → لا Migration.
- **📡 المراقبة (24 ساعة بعد النشر)**: زمن استجابة الـ API (p50/p95)، حجم
  الاستجابة الفعليّ، نسبة Cache HIT (إن وُجدت طبقة كاش وسيطة)، معدَّل
  الأخطاء، استخدام CPU، استخدام الذاكرة على عمليّة PHP-FPM/Laravel.
- **✅ نجاح 24 ساعة**: القائمة الموحَّدة + انخفاض فعليّ في متوسّط حجم
  الاستجابة مطابق لتقدير 1.1.

### 1.3 API Contract Snapshot

- **النوع**: Safety-net / Observability. **المسؤول**: Backend + Frontend.
- **🔧 التصنيف التشغيليّ**: Safe Change (قراءة فقط).
- **السبب**: عقد API ثابت أساسيّ لمشروع Headless؛ Regression قد يكون
  بالترويسات لا الـ JSON فقط.
- **التبعيّات**: يُفضَّل قبل أي بند يمسّ استجابة API (1.2، 2.7).
- **الإجراء**: لكل من الـ 7 endpoints، حفظ: جسم الاستجابة، **Status
  Code**، **كل الترويسات** (`Content-Type`، `Cache-Control`، `ETag`،
  `Last-Modified`، `Vary`، `X-RateLimit-*`) عبر `curl -s -D headers.txt
  -o body.json <url>`.
- **الوقت المتوقَّع**: 1.5–2 ساعة. **المخاطرة**: صفر.
- **الملفّات المتأثّرة**: `docs/roadmap/baselines/api-contracts/` (14
  ملفّاً).
- Backend؟ لا — Frontend؟ لا — Migration؟ لا — إعادة اختبار؟ لا.
- **معايير القبول**: 14 ملفّاً محفوظة، بتاريخ ومعرِّف commit.

### 1.4 إصلاح `SidebarNewsWidget` — تجاهل الـ locale (H1)

- **النوع**: Bug Fix. **المسؤول**: Frontend + QA.
- **🔧 التصنيف التشغيليّ**: Low Risk + Production Monitoring: Yes.
- **السبب**: محتوى عربيّ مؤكَّد على صفحة القسم الإنجليزيّة الحيّة.
- **التبعيّات**: يُنسَّق مع 1.5 (نفس نطاق EN).
- **الوقت المتوقَّع**: 30–45 دقيقة. **المخاطرة**: منخفضة جدّاً.
- **الملفّات المتأثّرة**: `sidebar-news-widget.tsx`، `reading-sidebar.tsx`،
  6 مواقع استدعاء.
- Backend؟ لا — Frontend؟ **نعم** — Migration؟ لا — إعادة اختبار؟ نعم.
- **معايير القبول**: `/en/category/60/public-news` يُظهِر عناوين
  إنجليزيّة حصراً.
- **🚩 Feature Flag**: `SIDEBAR_LOCALE_FIX_ENABLED`.
- **↩ خطّة التراجع**: تعطيل الـ flag → رجوع فوريّ لسلوك 'ar' الثابت →
  لا Migration.
- **📡 المراقبة**: معدَّل أخطاء console على الصفحات الستّة، معدَّل الزيارات
  المرتَدّة (bounce) على صفحات EN إن توفَّر تحليل.
- **✅ نجاح 24 ساعة**: القائمة الموحَّدة.

### 1.5 إصلاح بطاقات صفحة القسم الإنجليزيّة (امتداد H1)

- **النوع**: Bug Fix. **المسؤول**: Frontend + QA.
- **🔧 التصنيف التشغيليّ**: Low Risk + Production Monitoring: Yes.
- **السبب**: مكوّنات عربيّة على صفحة إنجليزيّة. **التبعيّات**: يُنسَّق
  مع 1.4.
- **الوقت المتوقَّع**: 1–2 ساعة. **المخاطرة**: منخفضة-متوسّطة.
- **الملفّات المتأثّرة**: `en/category/[id]/[name]/page.tsx`.
- Backend؟ لا — Frontend؟ **نعم** — Migration؟ لا — إعادة اختبار؟ نعم.
- **معايير القبول**: صفر نصّ عربيّ مرئيّ على الصفحة.
- **🚩 Feature Flag**: `EN_LOCALE_FIXES_ENABLED` (مشترك مع 1.4).
- **↩ خطّة التراجع**: تعطيل الـ flag → عودة للمكوّنات العربيّة (سلوك
  معروف) → لا Migration.
- **📡 المراقبة**: نفس 1.4.
- **✅ نجاح 24 ساعة**: القائمة الموحَّدة.

### 1.6 إضافة SEO Metadata كاملة لصفحات القسم

- **النوع**: SEO. **المسؤول**: Frontend + SEO.
- **🔧 التصنيف التشغيليّ**: Safe Change.
- **السبب**: صفر canonical/OG/Twitter/JSON-LD على صفحات عالية الحركة.
- **التبعيّات**: لا شيء.
- **الوقت المتوقَّع**: 2–3 ساعات. **المخاطرة**: منخفضة.
- **الملفّات المتأثّرة**: `(site)/category/[id]/[name]/page.tsx`،
  `en/category/[id]/[name]/page.tsx`.
- Backend؟ لا — Frontend؟ **نعم** — Migration؟ لا — إعادة اختبار؟ نعم.
- **معايير القبول**: canonical/OG/Twitter/BreadcrumbList صالحة (Google
  Rich Results Test) على الصفحتين.
- **↩ خطّة التراجع**: `git revert` — إضافة بحتة، لا Migration.

### 1.7 توثيق فخّ `revalidate` "الأقصر يفوز"

- **النوع**: Documentation. **المسؤول**: Frontend.
- **🔧 التصنيف التشغيليّ**: Safe Change.
- **التبعيّات**: لا شيء. **الوقت المتوقَّع**: 30–45 دقيقة. **المخاطرة**: صفر.
- **الملفّات المتأثّرة**: `docs/architecture/NEXTJS-CACHING-GOTCHAS.md`
  (جديد).
- Backend؟ لا — Frontend؟ لا — Migration؟ لا — إعادة اختبار؟ لا.
- **معايير القبول**: الملفّ موجود بالمثال الحقيقيّ.

### 1.8 تصحيح تعليقات/توثيق مضلِّل في الكود

- **النوع**: Documentation. **المسؤول**: Backend (Article.php) + Frontend
  (الباقي).
- **🔧 التصنيف التشغيليّ**: Safe Change.
- **التبعيّات**: لا شيء. **الوقت المتوقَّع**: 30 دقيقة. **المخاطرة**: صفر.
- **الملفّات المتأثّرة**: `Article.php`، `lib/epaper.ts`،
  `components/feed/feed-card.tsx`، `lib/engagement-bff.ts`.
- Backend؟ **نعم** — Frontend؟ **نعم** — Migration؟ لا — إعادة اختبار؟ لا.
- **معايير القبول**: كل تعليق يطابق السلوك الفعليّ حرفيّاً.

### 1.9 تصحيح وصف الموقع الإنجليزيّ (بيانات، لا كود)

- **النوع**: Data Fix. **المسؤول**: المحتوى/التحرير.
- **🔧 التصنيف التشغيليّ**: Safe Change.
- **التبعيّات**: لا شيء. **الوقت المتوقَّع**: 5 دقائق. **المخاطرة**: صفر.
- Backend؟ لا — Frontend؟ لا — Migration؟ لا — إعادة اختبار؟ لا.
- **معايير القبول**: `curl /api/v1/site?locale=en` يُظهِر وصفاً حقيقيّاً.
- **↩ خطّة التراجع**: تعديل الحقل مباشرة — رجوع فوريّ.

### 1.10 حذف المكوّنات الميتة المؤكَّدة

- **النوع**: Cleanup. **المسؤول**: Frontend.
- **🔧 التصنيف التشغيليّ**: Low Risk.
- **السبب**: 6 ملفّات ميتة، صفر مستوردين مؤكَّد.
- **التبعيّات**: لا شيء. **الوقت المتوقَّع**: 20–30 دقيقة. **المخاطرة**:
  منخفضة جدّاً.
- Backend؟ لا — Frontend؟ **نعم** — Migration؟ لا — إعادة اختبار؟ نعم.
- **معايير القبول**: `next build` ناجح، صفر إشارة للملفّات المحذوفة.
- **↩ خطّة التراجع**: `git revert`.

### 1.11 إصلاح Over-fetch في `(site)/layout.tsx` (بند مُستعاد — سقط سهواً عند إعادة الترقيم بين v2 وv3، يُعاد هنا بلا تغيير في محتواه الأصليّ)

- **النوع**: Optimization. **المسؤول**: Frontend.
- **🔧 التصنيف التشغيليّ**: Safe Change.
- **السبب**: `(site)/layout.tsx` يجلب تجميعة الرئيسية الكاملة (5 مناطق،
  ~70 كيلوبايت مُقاس فعليّاً في Sprint 0) على *كل* صفحة في الموقع
  لاستخدام `latest`+`breaking` فقط. مُوثَّق أصلاً في FRONTEND-AUDIT.md
  (M6) وFRONTEND-RUNTIME-AUDIT.md (توفير ~43 كيلوبايت مُقاس)، ويكتسب
  أولويّة أعلى الآن بعد رصد LCP=17.4s على الجوّال في خطّ أساس Sprint 0.
- **التبعيّات**: لا شيء.
- **الإصلاح**: استبدال `getHomepageFeed('ar')` في `(site)/layout.tsx`
  بـ `Promise.all([getLatestFeed('ar'), getBreakingFeed('ar')])`.
- **الوقت المتوقَّع**: 20–30 دقيقة. **المخاطرة**: منخفضة.
- **التأثير**: مرتفع وواسع — توفير ~43 كيلوبايت (مُقاس فعليّاً) على كل
  صفحة، وتحديث أسرع للشريط الإخباريّ (60s بدل 120s).
- **الملفّات المتأثّرة**: `app/(site)/layout.tsx`.
- Backend؟ لا — Frontend؟ **نعم** — Migration؟ لا — إعادة اختبار؟ نعم
  (التحقّق من أنّ الشريط الإخباريّ وشريط العاجل ما زالا يعملان في كل مكان
  يستخدم الـ layout).
- **معايير القبول**: `preview_network`/فحص حجم استجابة يُظهِر أنّ الصفحات
  غير الرئيسيّة لم تعد تجلب تجميعة الـ 5 مناطق؛ الشريط الإخباريّ وشريط
  العاجل يعملان بلا تغيّر مرئيّ على كل صفحة تستخدم `(site)/layout.tsx`.
- **↩ خطّة التراجع**: `git revert` — لا Migration.
- **📡 المراقبة**: حجم استجابة السيرفر لأي صفحة `(site)`، زمن تحميل
  الشريط الإخباريّ.
- **✅ نجاح 24 ساعة**: القائمة الموحَّدة + انخفاض فعليّ في حجم استجابة
  SSR على أي صفحة غير الرئيسية.

### Definition of Done — Sprint 1

- ✓ كل معايير القبول (11 بنداً، شامل 1.11 المُستعاد). ✓ `next build`/`tsc`/ESLint ناجحة.
- ✓ Backend: 2033 اختباراً — 2014 ناجح / 15 فاشل / 4 أخطاء، **مطابقة حرفيّاً**
  لخط الأساس المعروف مسبقاً (صفر تراجع من كل تغييرات Sprint 1 الخلفيّة).
- ✓ Contract Snapshot: أُعيد جلب كل الروابط التسعة وقورنت بنيويّاً (شكل
  الحقول لا القيم) بخط أساس 1.3 (commit `a3f3fb479`) — **صفر فروقات
  بنيويّة على كل التسعة**. الفروقات الأولى المُكتشَفة (03/08/09) كانت
  خطأ في إعادة بناء الرابط أثناء التحقّق نفسه (ترميز slug عربيّ + بادئة
  broadcast خاطئة)، وليست فرقاً حقيقيّاً — صُحِّحت وأعيد التحقّق.
- ⚠️ **Lighthouse — نتيجة مختلطة، موثَّقة بصدق**:
  - **Mobile**: تحسّن واضح ومتّسق مع منطق التغييرات — Performance 56→61،
    LCP 17.4s→7.8s.
  - **Desktop**: Performance 76 (خط الأساس) → 55/62/61 عبر ثلاث قراءات
    نظيفة منفصلة. الفحص العميق (network-requests + lcp-breakdown-insight)
    أظهر أنّ عنصر LCP **نفسه** (نفس الصورة، 84905 بايت، نفس
    `fetchpriority=high`، نفس وقت بدء الطلب) لم يتغيّر بين القراءتين؛
    الفارق الكامل تقريباً من `resourceLoadDuration` وحدها (310ms→2984ms)
    — تحميل نفس الملف الثابت من خادم Laravel المحلّي، لا من أي كود غيّره
    Sprint 1. عدد الطلبات الكلّي شبه ثابت (149→150). **الاستنتاج**: هذا
    تشويش قياس بيئيّ (تنافس I/O على جهاز التطوير المُستخدَم بكثافة طوال
    هذه الجلسة الطويلة)، لا تراجعاً برمجيّاً حقيقيّاً — معايير القبول
    البرمجيّة لكل بند من الـ11 لم تلمس مسار تقديم الصور الثابتة إطلاقاً.
    يُوصى بإعادة القياس على بيئة هادئة مخصّصة (Sprint لاحق أو قبل
    الانتقال للإنتاج) بدل اعتبارها معياراً قاطعاً هنا.
- ✓ صفر أخطاء console حرِجة.

**KPI**: صفر خلل locale حيّ متبقٍّ؛ تغطية SEO للقسم من 0%→100%؛ 9 لقطات
عقد API محفوظة (7 مخطَّطة + استُكمِلت لتغطية التسعة روابط الكاملة).

**الحالة**: Sprint 1 **مُغلَق** — كل البنود منفَّذة ومتحقَّق منها فرديّاً؛
التحذير الوحيد (Desktop Lighthouse) موثَّق أعلاه بدل تجاهله، وسبب جذريّ
مُرجَّح مدعوم بدليل مُقاس، لا افتراض.

---

# Sprint 2 — إصلاحات معماريّة وتحسينات أداء

### 2.1 تغليف `lib/sport/*.ts` و`reels.ts` بـ `React.cache()` ✅ منفَّذ

- **النوع**: Optimization. **المسؤول**: Frontend.
- **🔧 التصنيف التشغيليّ**: Safe Change.
- **التبعيّات**: لا شيء. **الوقت المتوقَّع**: 45 دقيقة – 1 ساعة.
  **المخاطرة**: منخفضة جدّاً.
- **الملفّات المتأثّرة**: `lib/sport/{games,stats,player}.ts`،
  `lib/reels.ts`.
- Backend؟ لا — Frontend؟ **نعم** — Migration؟ لا — إعادة اختبار؟ نعم.
- **معايير القبول**: نداء واحد بدل نداءين على `/sport/team/[id]`.
- **↩ خطّة التراجع**: `git revert`.

### 2.2 توحيد منطق الجلب المُرقَّم المُكرَّر ثلاث مرّات ✅ منفَّذ

- **النوع**: Refactor. **المسؤول**: Frontend + QA.
- **🔧 التصنيف التشغيليّ**: **High Risk** + Production Monitoring: Yes.
- **السبب**: منطق مطابق فعليّاً في 3 دوالّ.
- **التبعيّات**: يُفضَّل بعد 1.3.
- **الوقت المتوقَّع**: 2–3 ساعات. **المخاطرة**: متوسّطة.
- **الملفّات المتأثّرة**: `lib/feed.ts`، `lib/writer.ts`، `lib/search.ts`.
- Backend؟ لا — Frontend؟ **نعم** — Migration؟ لا — إعادة اختبار؟ **نعم،
  شامل**.
- **معايير القبول**: نتائج مطابقة حرفيّاً قبل/بعد على الثلاث.
- **🚩 Feature Flag**: نشر تدريجيّ صفحة بصفحة (البحث أوّلاً).
- **↩ خطّة التراجع**: `git revert` — لا Migration.
- **📡 المراقبة**: معدَّل أخطاء الترقيم/الفرز على `/search`، صفحة القسم،
  صفحة الكاتب؛ زمن استجابة `/articles` API.
- **✅ نجاح 24 ساعة**: القائمة الموحَّدة + صفر شكوى مستخدم عن نتائج بحث/
  ترقيم غير متوقَّعة.

### 2.3 توحيد منطق الجلب بـ cursor المُكرَّر ثلاث مرّات ✅ منفَّذ

- **النوع**: Refactor. **المسؤول**: Frontend.
- **🔧 التصنيف التشغيليّ**: Low Risk + Production Monitoring: Yes.
- **التبعيّات**: يُفضَّل بعد 1.3، بالتوازي مع 2.2.
- **الوقت المتوقَّع**: 1.5–2 ساعة. **المخاطرة**: منخفضة-متوسّطة.
- **الملفّات المتأثّرة**: `lib/feed.ts`.
- Backend؟ لا — Frontend؟ **نعم** — Migration؟ لا — إعادة اختبار؟ نعم.
- **معايير القبول**: نتائج مطابقة على أقسام الرئيسية، ودجت الوسوم،
  مقالات الكاتب.
- **↩ خطّة التراجع**: `git revert`.
- **📡 المراقبة**: معدَّل أخطاء على أقسام الرئيسية المتأثِّرة.
- **✅ نجاح 24 ساعة**: القائمة الموحَّدة.

### 2.4 إصلاح 3 مسارات BFF مُثبَّتة على `locale='ar'` ✅ منفَّذ

- **النوع**: Bug Fix (كامن). **المسؤول**: Frontend.
- **🔧 التصنيف التشغيليّ**: Safe Change.
- **التبعيّات**: لا شيء. **الوقت المتوقَّع**: 1 ساعة. **المخاطرة**:
  منخفضة جدّاً.
- **الملفّات المتأثّرة**: `comments/route.ts`، `live-updates/route.ts`،
  `epaper/[idslug]/route.ts`.
- Backend؟ لا — Frontend؟ **نعم** — Migration؟ لا — إعادة اختبار؟ نعم.
- **معايير القبول**: السلوك العربيّ بلا تغيير؛ يقبلن `locale` صراحة.
- **↩ خطّة التراجع**: `git revert`.

### 2.5 إضافة `error.tsx`/`global-error.tsx` وحدود `not-found.tsx` الناقصة ✅ منفَّذ

- **النوع**: Bug Fix / Resilience. **المسؤول**: Frontend + QA.
- **🔧 التصنيف التشغيليّ**: Low Risk.
- **التبعيّات**: لا شيء. **الوقت المتوقَّع**: 2–3 ساعات. **المخاطرة**:
  منخفضة.
- **الملفّات المتأثّرة**: `app/error.tsx`، `(reels)/not-found.tsx`،
  `newspaper/not-found.tsx` (جديدة).
- Backend؟ لا — Frontend؟ **نعم** — Migration؟ لا — إعادة اختبار؟ نعم.
- **معايير القبول**: رابط غير موجود يُظهِر تصميم 404 الموحَّد.
- **↩ خطّة التراجع**: `git revert`.

### 2.6 توحيد تحليل شجرة الأقسام المُكرَّر مرّتين ✅ منفَّذ

- **النوع**: Refactor. **المسؤول**: Frontend.
- **🔧 التصنيف التشغيليّ**: Low Risk.
- **التبعيّات**: يُفضَّل بعد 1.3. **الوقت المتوقَّع**: 1–1.5 ساعة.
  **المخاطرة**: منخفضة-متوسّطة.
- **الملفّات المتأثّرة**: `lib/categories.ts`، `lib/feed.ts`.
- Backend؟ لا — Frontend؟ **نعم** — Migration؟ لا — إعادة اختبار؟ نعم.
- **معايير القبول**: نموذج الكاتب وحلّ id→slug بلا تغيير ملحوظ.
- **↩ خطّة التراجع**: `git revert`.

### 2.7 قرار `is_featured` على مورد قائمة المقالات ✅ منفَّذ (قُرِّر ب — انظر DEC-001)

- **النوع**: Architecture Decision + Bug Fix. **المسؤول**: Backend (أ) /
  Frontend (ب).
- **🔧 التصنيف التشغيليّ**: Low Risk + Production Monitoring: Yes (إن
  اختير المسار أ).
- **التبعيّات**: يعتمد على 1.3 (لقطة العقد قبل تعديل Resource).
- **الوقت المتوقَّع**: (أ) 1 ساعة / (ب) 15 دقيقة. **المخاطرة**: منخفضة.
- **الملفّات المتأثّرة**: (أ) `PublicArticleListItemResource.php` / (ب)
  `en/category/[id]/[name]/page.tsx`.
- Backend؟ **نعم إن أ** — Frontend؟ **نعم في الحالتين** — Migration؟ لا
  — إعادة اختبار؟ نعم.
- **معايير القبول**: `is_featured` يعكس حالة تحرير حقيقيّة.
- **↩ خطّة التراجع (أ)**: إزالة الحقل → إعادة نشر → لا Migration.
- **📡 المراقبة (إن أ)**: صفر تغيّر في حجم/بنية استجابة القوائم الأخرى
  المستهلِكة لنفس الـ Resource.
- **🔗 القرار/ADR**: **DEC-001** في
  [DECISION-LOG.md](../architecture/DECISION-LOG.md) — لا ADR (قرار
  تنفيذيّ صغير النطاق).

### 2.8 إصلاح بقيّة أسماء العلامة القديمة في الريلز ✅ منفَّذ

- **النوع**: Data/Cleanup. **المسؤول**: Frontend.
- **🔧 التصنيف التشغيليّ**: Safe Change.
- **السبب**: 3 مواقع متبقّية؛ تحقّق أوّلاً من `(site)/page.tsx:137` (رُصِد
  تعديل خارجيّ محتمَل).
- **التبعيّات**: لا شيء. **الوقت المتوقَّع**: 15 دقيقة. **المخاطرة**: صفر.
- Backend؟ لا — Frontend؟ **نعم** — Migration؟ لا — إعادة اختبار؟ لا.
- **معايير القبول**: الأربعة تعرض "القلعة نيوز" حصراً.
- **↩ خطّة التراجع**: `git revert`.

### 2.9 توحيد قاعدة تمرير الـ locale عبر `lib/*.ts` ✅ منفَّذ

- **النوع**: Refactor / Consistency. **المسؤول**: Frontend.
- **🔧 التصنيف التشغيليّ**: **High Risk** (نطاق واسع نسبيّاً) —
  **يُنصَح بتنفيذه على دفعات صغيرة بدل نشرة واحدة (Maintenance Window
  غير ضروريّة إن نُفِّذ تدريجيّاً)**.
- **التبعيّات**: يُفضَّل بعد 2.2/2.3. **الوقت المتوقَّع**: 3–4 ساعات، على
  دفعات. **المخاطرة**: متوسّطة.
- **الملفّات المتأثّرة**: `site-settings.ts`، `categories.ts`، وغيرها.
- Backend؟ لا — Frontend؟ **نعم** — Migration؟ لا — إعادة اختبار؟ نعم،
  تدريجيّ.
- **معايير القبول**: قاعدة واحدة موثَّقة ومُطبَّقة.
- **↩ خطّة التراجع**: `git revert` لكل commit صغير على حدة.

### 2.10 توحيد استخدام ترويسة المصادقة الداخليّة ✅ منفَّذ (7 ملفّات — انظر رسالة الـcommit للتفصيل)

- **النوع**: Consistency / Performance. **المسؤول**: Frontend.
- **🔧 التصنيف التشغيليّ**: Safe Change.
- **التبعيّات**: لا شيء. **الوقت المتوقَّع**: 1 ساعة. **المخاطرة**:
  منخفضة جدّاً.
- **الملفّات المتأثّرة**: 8 ملفّات `lib/*.ts` (انظر التقرير الأوّل).
- Backend؟ لا — Frontend؟ **نعم** — Migration؟ لا — إعادة اختبار؟ لا.
- **معايير القبول**: كل النداءات تحمل `X-Internal-Token` عند توفّره.
- **↩ خطّة التراجع**: `git revert`.

### Definition of Done — Sprint 2

- ✓ كل معايير القبول (10 بنود، منفَّذة جميعها). ✓ `next build`/`tsc`/ESLint
  ناجحة بعد كل بند.
- ✓ Contract Snapshot: أُعيد جلب التسعة روابط وقورنت بنيويّاً بخط الأساس
  (commit `a3f3fb479`) — **صفر فروقات بنيويّة**.
- ✓ Backend: الاختبار الكامل بعد كل تغييرات Sprint 2 — 2033 اختباراً،
  **2018 ناجح / 15 فاشل** (تحسّن عن خط أساس Sprint 1: 2014 ناجح/15
  فاشل/4 أخطاء — أي +4 نجاح، صفر تراجع). تحقّقتُ من الخمسة عشر فشلاً
  فرداً: **خمسة** امتداد لمشكلة تنسيق الرابط القانونيّ المُوثَّقة مسبقاً
  في Sprint 1 (الاختبارات تتوقّع الشكل السالف بالسلَغ، الكود يُعيد
  الشكل الجديد بالـid فقط، عمداً وصحيحاً — `PublicArticleSeoTest`،
  `ArticleRedirectTest`، `NewsSeoTest`×2، `SeoDeliveryTest`)، **وعشرة**
  في مناطق لم يُلمَسها أيّ بند من Sprint 1 أو Sprint 2 إطلاقاً (تنقية
  CDN، عقد محرِّر المقال×3، تاريخ الروابط، عقد أخطاء إدارة المستخدمين،
  انتقال حالة حملة إعلانيّة، سياسة/تنزيل الجريدة الرقميّة، إشعارات
  الكتّاب). صفر علاقة بأيّ ملفّ عدَّلته.
- ✓ صفر أخطاء console حرِجة (تحقّق حيّ لكل صفحة مُتأثِّرة خلال كل بند).

**KPI**: تقليص تطبيقات الجلب المُكرَّر من 6→2 (تحقَّق: 2.2+2.3)؛ صفر
جلب مضاعَف متبقٍّ؛ 100% من الصفحات لديها حدّ خطأ مخصَّص (2.5).

**الحالة**: Sprint 2 **مُغلَق** — كل البنود العشرة منفَّذة ومتحقَّق منها
فرديّاً؛ حالة واحدة استدعت تصحيح مسار أثناء التنفيذ (2.7 — انظر
DEC-001) موثَّقة بالكامل بدل تجاهلها.

---

# Sprint 3 — تحسينات وقرارات استراتيجيّة

### 3.1 مشروع توحيد المكوّنات (البطاقات/الهيرو/الترقيم) — مرحليّ

- **النوع**: Architecture Decision + Refactor (كبير). **المسؤول**:
  Frontend + Product/Design.
- **🔧 التصنيف التشغيليّ**: **High Risk** + **Maintenance Window
  Recommended** (للمشروع الكامل فقط، لا الخطوة الأولى) + Production
  Monitoring: Yes.
- **شرط البدء**: قرار Product/Design أوّلاً.
- **التبعيّات**: يستفيد من نتيجة Performance Budget (0.4).
- **الوقت المتوقَّع**: الخطوة الأولى: 1–2 يوم؛ الكامل: أسابيع.
- **المخاطرة**: متوسّطة-مرتفعة للكامل؛ منخفضة للخطوة الأولى.
- **الملفّات المتأثّرة**: الخطوة الأولى: `HorizontalArticleCard.tsx`،
  `FeaturedCategoryCard.tsx`.
- Backend؟ لا — Frontend؟ **نعم** — Migration؟ لا — إعادة اختبار؟ نعم،
  شامل بصريّاً.
- **معايير القبول (الخطوة الأولى)**: كل مواقع الاستخدام تعرض نفس التصميم.
- **↩ خطّة التراجع**: فرع طويل الأمد مع مراجعة بصريّة شاملة قبل الدمج
  لـ`main` — **الأصعب في الوثيقة كاملة** للتراجع الجزئيّ بعد اكتمال الدمج.
- **📡 المراقبة**: تقارير أخطاء بصريّة من المستخدمين، معدَّل الزيارات
  المرتَدّة على الصفحات المتأثِّرة (قبل/بعد).
- **✅ نجاح 24 ساعة**: القائمة الموحَّدة + صفر تقرير مستخدم عن تصميم مكسور.
- **🔗 القرار/ADR**: **DEC-004** في DECISION-LOG.md — **مرشَّح لِـ ADR
  جديد (E9، تالي رقم متاح في سلسلة الامتدادات E1/E2/E5–E8) إن اختير
  المشروع الكامل**؛ لا يُنشأ قبل القرار.

### 3.2 إصلاح تحذير Recharts (`/bourse`) ✅ منفَّذ

- **النوع**: Bug Fix / Polish. **المسؤول**: Frontend.
- **🔧 التصنيف التشغيليّ**: Low Risk.
- **التبعيّات**: يعتمد على رقم CLS الفعليّ من 0.2 (قد تُرفَع الأولويّة).
- **الوقت المتوقَّع**: 2–3 ساعات. **المخاطرة**: منخفضة-متوسّطة.
- **الملفّات المتأثّرة**: مكوّن الرسم البيانيّ في `/bourse`.
- Backend؟ لا — Frontend؟ **نعم** — Migration؟ لا — إعادة اختبار؟ نعم.
- **معايير القبول**: صفر تحذيرات Recharts في console.
- **↩ خطّة التراجع**: `git revert`.

### 3.3 إصلاح عنوان صفحة البحث ✅ منفَّذ

- **النوع**: SEO / Polish. **المسؤول**: Frontend.
- **🔧 التصنيف التشغيليّ**: Safe Change.
- **التبعيّات**: لا شيء. **الوقت المتوقَّع**: 30 دقيقة. **المخاطرة**: صفر.
- Backend؟ لا — Frontend؟ **نعم** — Migration؟ لا — إعادة اختبار؟ لا.
- **معايير القبول**: `<title>` يتضمّن نصّ الاستعلام.
- **↩ خطّة التراجع**: `git revert`.

### 3.4 إصلاح/حذف أزرار متاجر التطبيقات غير الفعّالة ✅ منفَّذ (قرار: إخفاء كامل)

- **النوع**: Content Decision. **المسؤول**: Frontend + Product.
- **🔧 التصنيف التشغيليّ**: Safe Change.
- **التبعيّات**: قرار منتجيّ أوّلاً. **الوقت المتوقَّع**: 15 دقيقة.
  **المخاطرة**: صفر.
- Backend؟ لا — Frontend؟ **نعم** — Migration؟ لا — إعادة اختبار؟ لا.
- **معايير القبول**: روابط حقيقيّة أو إخفاء كامل.

### 3.5 توحيد مساعد الكاش المؤقّت المُكرَّر 3 مرّات ✅ منفَّذ

- **النوع**: Refactor / Cleanup. **المسؤول**: Frontend.
- **🔧 التصنيف التشغيليّ**: Safe Change.
- **التبعيّات**: لا شيء. **الوقت المتوقَّع**: 45 دقيقة. **المخاطرة**: منخفضة.
- Backend؟ لا — Frontend؟ **نعم** — Migration؟ لا — إعادة اختبار؟ نعم.
- **معايير القبول**: سلوك التطوير المحليّ بلا تغيير.
- **↩ خطّة التراجع**: `git revert`.

### 3.6 قرار مصير endpoints غير المُستخدَمة ✅ منفَّذ (قرار: الإبقاء كما هما — انظر DEC-002)

- **النوع**: Architecture Decision. **المسؤول**: Backend + Product.
- **🔧 التصنيف التشغيليّ**: Safe Change (قرار) / Low Risk (إن حُذِف).
- **التبعيّات**: لا شيء. **الوقت المتوقَّع**: قرار فقط؛ حذف 1–2 ساعة إن
  اختير.
- Backend؟ **نعم إن حُذِف** — Frontend؟ لا — Migration؟ لا — إعادة
  اختبار؟ نعم إن حُذِف.
- **معايير القبول**: قرار مكتوب.
- **↩ خطّة التراجع (إن حُذِف)**: استرجاع من git — لا Migration.
- **🔗 القرار/ADR**: **DEC-002** في DECISION-LOG.md — لا ADR (تشغيليّ).

### 3.7 إصلاح الجلب المضاعَف في `en/author/[id]` ✅ منفَّذ

- **النوع**: Optimization. **المسؤول**: Frontend.
- **🔧 التصنيف التشغيليّ**: Safe Change.
- **التبعيّات**: لا شيء. **الوقت المتوقَّع**: 30 دقيقة. **المخاطرة**: منخفضة.
- Backend؟ لا — Frontend؟ **نعم** — Migration؟ لا — إعادة اختبار؟ لا.
- **معايير القبول**: نداء واحد فقط لكل تحميل صفحة.
- **↩ خطّة التراجع**: `git revert`.

### 3.8 معالجة معرّفات الأقسام المُثبَّتة بالكود (Multi-Site/White-Label) 🟡 اتّجاه مُقرَّر، تنفيذ مؤجَّل (انظر DEC-003)

- **النوع**: Architecture Decision (استراتيجيّ). **المسؤول**: Product +
  Backend + Frontend.
- **🔧 التصنيف التشغيليّ**: **High Risk** + **Maintenance Window
  Recommended** إن اختير المضيّ.
- **شرط البدء**: مشروط بخطّة فعليّة لـ Multi-Site/White-Label.
- **التبعيّات**: قرار استراتيجيّ خارجيّ. **الوقت المتوقَّع**: أيّام.
  **المخاطرة**: مرتفعة إن نُفِّذ دون خطّة.
- Backend؟ محتمل — Frontend؟ **نعم** — Migration؟ محتمل — إعادة اختبار؟
  شامل.
- **معايير القبول**: لا ينطبق قبل قرار المضيّ.
- **🔗 القرار/ADR**: **DEC-003** في DECISION-LOG.md — **مرشَّح لِـ ADR
  جديد (E9 أو E10، أيّهما لم يُستهلَك بعد إن نُفِّذ 3.1 الكامل أوّلاً)
  إن اختير المضيّ**؛ لا يُنشأ قبل القرار.

### 3.9 اعتماد قياس Cold-vs-Warm Cache كممارسة قياسيّة دائمة ✅ منفَّذ

- **النوع**: Process / Methodology. **المسؤول**: الفريق كاملاً.
- **🔧 التصنيف التشغيليّ**: لا ينطبق (سياسة، لا نشر).
- **التبعيّات**: لا شيء. **الوقت المتوقَّع**: سياسة مستمرّة.
- Backend؟ لا — Frontend؟ لا — Migration؟ لا — إعادة اختبار؟ ينطبق على
  كل بند أداء مستقبليّ.

### Definition of Done — Sprint 3

- ✓ كل بند نُفِّذ أو أُغلِق بقرار موثَّق:
  - **3.1**: مؤجَّل — DEC-004 بقي 🟡 معلَّق بقرار المستخدم (تخطٍّ صريح،
    لا حذف قرار).
  - **3.2–3.5، 3.7، 3.9**: منفَّذة بالكامل، مُتحقَّق منها فرديّاً.
  - **3.6**: قرار مُتَّخَذ (DEC-002 — الإبقاء كما هما)، صفر تغيير كود
    (الخيار المُختار لا يتطلّب كوداً).
  - **3.8**: اتّجاه استراتيجيّ مُقرَّر جزئيّاً (DEC-003 — "نعم لاحقاً")
    لكن **التنفيذ الفعليّ يبقى بنداً مفتوحاً** — لا تصميم ملموس متوفّر
    بعد؛ لم يُخترَع بنية بيانات/معماريّة من طرف واحد.
- ✓ `next build`/`tsc`/ESLint ناجحة بعد كل بند وفي فحص شامل ختاميّ —
  صفر أخطاء (5 تحذيرات ESLint سابقة الوجود، من جلسة خارجيّة، غير
  مُتعلِّقة بأيّ ملفّ لمسته Sprint 2/3).
- ✓ صفر تراجع Backend — Sprint 3 لم يُغيِّر أيّ ملفّ Backend إطلاقاً (لا
  حاجة لإعادة الاختبار الكامل؛ نتيجة إغلاق Sprint 2 تبقى صحيحة).
- ✓ صفر تحذيرات Recharts متبقّية (3.2 مُنفَّذ ومُتحقَّق منه ببناءين
  نظيفين منفصلين).

**KPI**: خفض أنظمة الترقيم المستقلّة من 4→≤3 (أو ≤2 إن اكتمل 3.1) — **لم
يتحقَّق** (3.1 مؤجَّل، لا تراجع — الحالة الأصليّة 4 تبقى كما هي، متوقَّعة
تماماً بصياغة الـKPI نفسها). صفر `href="#"` غير مبرَّر — **تحقَّق** (3.4).
قرار موثَّق لكل من 3.6 و3.8 — **تحقَّق** (كلاهما في DECISION-LOG.md).

**الحالة**: Sprint 3 **مُغلَق** — كل بند إمّا مُنفَّذ ومُتحقَّق منه، أو
مُغلَق بقرار موثَّق صريح (لا قرار ضمنيّ ولا صمت). البندان الوحيدان
بتنفيذ غير مكتمل (3.1، 3.8) كلاهما بانتظار مدخلات خارج نطاق هندسة هذه
الجلسة (قرار Product/Design لـ3.1، تصميم مفصَّل لـ3.8) — موثَّقان صراحةً
لا مُهمَلان.

---

## Risk Matrix (مع Blast Radius)

| البند | المخاطرة | إمكانية الرجوع | Blast Radius | التصنيف التشغيليّ |
|---|---|---|---|---|
| 1.2 Unicode | منخفضة | عالية | **كامل الـ API** | Safe Change + Monitoring |
| 1.3 Contract Snapshot | صفر | لا ينطبق | لا شيء | Safe Change |
| 1.4 Sidebar Locale | منخفضة | عالية | صفحات EN (6 مواقع) | Low Risk + Monitoring |
| 1.5 EN Category Cards | منخفضة-متوسّطة | عالية | صفحة القسم EN | Low Risk + Monitoring |
| 1.6 Category SEO | منخفضة | عالية | صفحات القسم (ar+en) | Safe Change |
| 1.10 حذف مكوّنات ميتة | منخفضة جدّاً | متوسّطة | لا شيء حيّ | Low Risk |
| 2.1 `cache()` sport/reels | منخفضة جدّاً | عالية | صفحات الرياضة+ريل | Safe Change |
| 2.2 توحيد الترقيم | متوسّطة | متوسّطة | قسم+كاتب+بحث | **High Risk** + Monitoring |
| 2.3 توحيد cursor | منخفضة-متوسّطة | متوسّطة | أقسام الرئيسية | Low Risk + Monitoring |
| 2.4 إصلاح locale BFF | منخفضة جدّاً | عالية | لا شيء حيّ اليوم | Safe Change |
| 2.7 `is_featured` (أ) | منخفضة | عالية | كل مستهلكي Resource | Low Risk + Monitoring |
| 2.9 توحيد locale convention | متوسّطة | متوسّطة | كامل `lib/` تقريباً | **High Risk** |
| 3.1 توحيد المكوّنات (كامل) | **مرتفعة** | **منخفضة** | **كل صفحة محتوى** | **High Risk** + Maintenance Window |
| 3.2 Recharts | منخفضة-متوسّطة | عالية | `/bourse` فقط | Low Risk |
| 3.8 معرّفات الأقسام | **مرتفعة** | منخفضة | استراتيجيّ | **High Risk** + Maintenance Window |

**قراءة الجدول**: 1.2 لها Blast Radius واسع رغم بساطتها — تستحقّ مراقبة
لصيقة بعد النشر رغم انخفاض مخاطرتها التقنيّة. 3.1 و3.8 لا تبدأ بلا
موافقة صريحة ونطاق محدَّد، وتستحقّان نافذة صيانة مُعلَنة مسبقاً لا نشراً
عاديّاً.

---

## ملخّص سريع

| المرحلة | عدد البنود | الوقت الإجماليّ التقريبيّ | يحتاج Backend |
|---|---|---|---|
| Sprint 0 | 4 | ~2–2.5 ساعة | لا |
| Sprint 1 | 10 | ~2–2.5 يوم عمل | بندان (1.2، 1.8-جزئيّ) |
| Sprint 2 | 10 | ~3.5–4.5 أيّام عمل | بند واحد مشروط (2.7-أ) |
| Sprint 3 | 9 | متفاوت؛ 3.1 و3.8 مشاريع مستقلّة | بند واحد مشروط (3.6) |

---

## دورة الوثائق الكاملة

```
تدقيق (3 تقارير) → خارطة الطريق (هذه الوثيقة) + DECISION-LOG.md
    → تنفيذ Sprint 0 (Baseline)
    → تنفيذ Sprint 1
    → POST-SPRINT-1-VALIDATION.md   (مقارنة قبل/بعد مقابل Baseline Sprint 0)
    → تنفيذ Sprint 2
    → تنفيذ Sprint 3
    → FINAL-IMPLEMENTATION-REPORT.md   (إغلاق الدورة كاملة)
```

**`POST-SPRINT-1-VALIDATION.md`** — بعد تنفيذ Sprint 1 فعليّاً فقط.
مقارنة قبل/بعد ضدّ Baseline (Sprint 0) حصراً.

**`FINAL-IMPLEMENTATION-REPORT.md`** — بعد اكتمال كل المراحل (0–3):
ما نُفِّذ، ما أُجِّل ولماذا، Baseline مقابل النتيجة النهائيّة، الدروس
المستفادة، قائمة الديون التقنيّة المتبقّية صراحة.

**لا تُكتَب أيّ منهما الآن.**
